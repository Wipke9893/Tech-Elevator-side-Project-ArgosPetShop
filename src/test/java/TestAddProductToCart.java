import org.junit.Test;

import java.util.Map;
import java.util.Scanner;

import static org.junit.Assert.assertEquals;

// public class TestAddProductToCart {
//    @Test
//    void testAddProductToCart() {
//        Inventory inventory = new Inventory(); // Populate this manually or mock it
//        double cart = 0.0;
//        Map<String, Double> products = inventory.getProductsByCategory("bed");
//
//        double newCartTotal = Methods.addProductToCart("small bed", "small bed", 1, inventory, cart);
//        assertEquals(20.0, newCartTotal, 0.001);
//
//        newCartTotal = addProductToCart("bed", "medium bed", 2, inventory, newCartTotal);
//        assertEquals(80.0, newCartTotal, 0.001);
//    }
// }